import serial
import time

port = serial.Serial("""...""")

while !goal:
	while !seeGreen:
		port.write("turn counterclockwise 60")
		if angle < 30:
			port.write("forward short time")
	while seeGreen:
		port.write("forward a short time")
		if obstacle
			# perform avoidance maneuver
