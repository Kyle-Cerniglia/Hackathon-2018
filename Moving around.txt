import serial
import time

port = serial.Serial("\dev\ttyAMA0",baudrate=9600,timeout=3.0)

while not(goal):
	while not(seeGreen):
		port.write("CC60")
		if angle < 30:
			port.write("f")
	while seeGreen:
		port.write("f")
		if obstacle
			# perform avoidance maneuver
